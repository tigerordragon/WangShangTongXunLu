<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.addressbook.mapper.RefreshTokenMapper">

    <resultMap id="refreshTokenResultMap" type="com.addressbook.entity.RefreshTokenRecord">
        <constructor>
            <idArg column="token" javaType="string"/>
            <arg column="student_id" javaType="java.lang.Long"/>
            <arg column="expires_at" javaType="java.time.Instant"/>
            <arg column="active" javaType="_boolean"
                 typeHandler="org.apache.ibatis.type.BooleanTypeHandler"/>
        </constructor>
    </resultMap>

    <insert id="insert" parameterType="com.addressbook.entity.RefreshTokenRecord">
        INSERT INTO refresh_token (token, student_id, expires_at, active)
        VALUES (#{token}, #{studentId}, #{expiresAt}, #{active})
    </insert>

    <update id="update" parameterType="com.addressbook.entity.RefreshTokenRecord">
        UPDATE refresh_token SET
            student_id = #{studentId},
            expires_at = #{expiresAt},
            active = #{active}
        WHERE token = #{token}
    </update>

    <select id="selectByToken" resultMap="refreshTokenResultMap">
        SELECT token, student_id, expires_at, active FROM refresh_token WHERE token = #{token}
    </select>

    <update id="revoke">
        UPDATE refresh_token SET active = 0 WHERE token = #{token}
    </update>
</mapper>
